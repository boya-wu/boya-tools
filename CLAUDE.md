# 專案 AI 啟動規則

> `CLAUDE.md` 與 `AGENTS.md` 必須逐字一致。兩份都是 Obsidian-Vault 規則的啟動投影。

每次開始本專案任務前：

1. 先讀 `C:\Users\Administrator\Documents\GitHub\Obsidian-Vault\AGENTS.md`，並依其啟動流程完成 Vault 全域規則、Luna 人格與未解提醒的載入。
2. 再讀 `C:\Users\Administrator\Documents\GitHub\Obsidian-Vault\rules\12-boya-tools\README.md`；依它的指引，只讀這次工作需要的專案規則。
3. 需要跨專案通用規則時，再讀 `C:\Users\Administrator\Documents\GitHub\Obsidian-Vault\rules\01-跨專案通用規則\` 的對應檔案。

Vault 根目錄的 `AGENTS.md` 管全域啟動流程與強制規則；`rules\12-boya-tools\README.md` 管本專案的規則導讀。兩者正本都在 Obsidian-Vault，本 repo 的 `CLAUDE.md` 與 `AGENTS.md` 只是啟動投影。規則需要修改時更新 Vault，不在 repo 建立或維護 `.cursor`。
